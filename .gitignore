node_modules/
dist/
out/
*.dmg
*.AppImage
*.exe
.DS_Store

# Engine binaries and libraries (downloaded via scripts/setup-engine.sh)
bin/iimagine-engine
bin/iimagine-engine.exe
bin/*.dylib
bin/*.so
bin/*.so.*
bin/*.dll
bin/*.metal
bin/.engine-version

# Stable Diffusion build artifacts
.sd-build/

# Stable Diffusion engine (removed from public repo, stays local)
sd-engine-manager.js
scripts/build-sd-engine.sh

# Paid plugins (distributed via marketplace, not open source)
plugins/client-workspace/
plugins/cortex-lite/
plugins/legal-companion/

# Credentials and secrets
oauth-credentials.json
config.json

# Internal tooling (not part of the open source project)
scripts/upload-to-blob.js

# Internal docs (roadmap, release process, historical migrations)
docs/internal/

# Paid plugin documentation
docs/client-workspace-plugin.md
docs/cortex-lite-plugin.md
docs/legal-companion-plugin.md
docs/client-coms-example.md

# Kiro IDE configuration
.kiro/

# Whisper binary (downloaded separately)
bin/whisper/

# Cloud plugin (not ready until web app endpoints deployed)
plugins/iimagine-cloud/

# Docs (internal planning, not part of open source release)
docs/

# Python virtual environments in plugins
plugins/**/.venv/
plugins/**/__pycache__/

# Paid/proprietary plugins (distributed via marketplace)
plugins/cortex/
plugins/business-os/

# Experimental (not ready for public release)
plugins/shopping-list/
plugins/privacy-proxy/
